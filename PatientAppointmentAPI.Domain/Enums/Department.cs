namespace PatientAppointmentAPI.Domain.Enums
{
    /// <summary>
    /// An enum to represent departments with some example values. 
    /// Eventually this would likely become its own entity to allow more detail about each dept
    /// </summary>
    public enum Department
    {
        None,
        Cardiology,
        Neurology,
        Immunology,
        GeneralPractice,
    }
}
